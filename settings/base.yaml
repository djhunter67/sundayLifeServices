application:
  port: 8000

mongo:
 uri: "mongodb://djhunter67:<password>"
 db: "sundayLife"
 collection: "test"
 require_auth: true
 pool_size: 1
 connection_timeout: 1

redis:
  uri: "redis://:<password>"
  pool_size: 1
  pool_max_open: 16
  pool_max_idle: 8
  pool_timeout_seconds: 1
  pool_expire_seconds: 60

sqlite:
  path: "senzor.db"
  schema: "static/schema.sql"
  pool_size: 1
  connection_timeout: 1