app = "sundaylifeservices"

primary_region = "ewr"

[build]
  builder = "paketobuildpacks/builder:base"

[env]
  RUST_LOG = "info"

[http_service]
  internal_port = 8000
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0